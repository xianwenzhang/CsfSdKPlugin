<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.csf.csfsdktest">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MainVerticalActivity"></activity>

        <activity
            android:name=".MainLandScapeActivity"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:screenOrientation="landscape"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen"></activity>

        <!--外网环境下的AccessKey和SecretKey-->
        <!--数库AccessKey-->
        <meta-data
            android:name="CSF_ACCESSKEY"
            android:value="cc21849432aedced6163a555bd7f58ab"></meta-data>

        <!--数库SecretKey-->
        <meta-data
            android:name="CSF_SECRETKEY"
            android:value="hIOEJ7bKpntFfg0lFketQ9/JSlw="></meta-data>

    </application>
    <uses-permission android:name="android.permission.INTERNET" />
</manifest>
